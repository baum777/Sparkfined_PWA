# Web-Push (VAPID)
VAPID_PUBLIC_KEY=
VAPID_PRIVATE_KEY=
VAPID_CONTACT=mailto:you@example.com
# Client: stelle die Public Key zus?tzlich als Vite-Var bereit
VITE_VAPID_PUBLIC_KEY=

# Upstash Redis (for push subscriptions & alerts queue)
UPSTASH_REDIS_REST_URL=
UPSTASH_REDIS_REST_TOKEN=

# AI Provider Keys (Server-side only)
OPENAI_API_KEY=
ANTHROPIC_API_KEY=
XAI_API_KEY=
# Optional Guardrails
AI_MAX_COST_USD=0.25
AI_CACHE_TTL_SEC=300

# Data Provider (for OHLC)
DEXPAPRIKA_BASE=https://api.dexpaprika.com
DEXPAPRIKA_API_KEY=

# Cron (Vercel Scheduler) ? ruft periodisch die Server-Evaluation auf
# In Vercel ? Settings ? Scheduler: GET /api/rules/eval-cron  (z.B. jede Minute)

# Journal ben?tigt keine weiteren ENV, nutzt Upstash (E-6)
# Export erfolgt ?ber /api/journal/export (fmt=json|md)
