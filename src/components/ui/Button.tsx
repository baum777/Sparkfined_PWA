/**
 * @deprecated Use `import { Button } from '@/design-system'` instead.
 * This file remains to keep legacy imports working during migration.
 * TODO: design-system: repoint all call sites to '@/design-system' and delete by next release train.
 */
export { Button as default } from '@/design-system/components/Button/Button'
export type {
  ButtonProps,
  ButtonSize,
  ButtonVariant,
} from '@/design-system/components/Button/Button'
