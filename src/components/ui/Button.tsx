// design-system: compatibility shim (Step 1). Existing imports keep working.
export {
  Button,
  type ButtonProps,
  type ButtonSize,
  type ButtonVariant,
} from '@/design-system/components/Button/Button'

export default Button
