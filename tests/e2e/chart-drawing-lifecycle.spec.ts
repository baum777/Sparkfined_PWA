// Placeholder E2E for drawing lifecycle (create/edit/delete/undo) once Playwright browsers are available.
// Currently skipped due to missing browser binaries in the environment.
import { test } from './fixtures/baseTest'

test.describe.skip('chart drawing lifecycle (e2e)', () => {
  test('create → select → move → delete drawing', async () => {
    // Implement when Playwright runtime is available in CI runners.
  })
})
